interface:
  display_name: "Daisy Bridge"
  short_description: "Repo-specific patterns and workflows for daisy-bridge"
  default_prompt: "Use the daisy-bridge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true